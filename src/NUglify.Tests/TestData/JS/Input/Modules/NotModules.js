// this is not ES6 code, it's just someone using "modules" "import" and "export" as variable names.
// the only errors should be a "don't do that" warning.

// assign a value - statement starts with "module"
module = 42;

// implicit semicolon between the two because of the line break
module
"string literal with line break"
