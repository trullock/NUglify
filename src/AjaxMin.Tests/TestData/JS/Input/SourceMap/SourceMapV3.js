// this won't actually contribute any source code to the project;
// I'm specifying two other files on the command-line -- the same
// files used in the ScriptSharpMap test. That way we can use the same
// resulting output.
