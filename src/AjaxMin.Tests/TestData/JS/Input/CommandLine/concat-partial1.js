// this is a partial script. 
// we DON'T want a semicolon inserted between it, so we're 
// going to end the file with a special comment followed by
// a bunch of multi-line whitespace to make sure the regex is working.
var obj = {
//;
    
  
